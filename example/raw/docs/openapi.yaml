# Generated by craftgo. DO NOT EDIT.
components:
  schemas:
    DownloadReq:
      type: object
    Event:
      properties:
        kind:
          type: string
        payload:
          type: string
      required:
      - kind
      - payload
      type: object
    EventsRespBody:
      $ref: '#/components/schemas/Event'
    HealthReq:
      type: object
    HealthResp:
      properties:
        status:
          maxLength: 16
          minLength: 1
          type: string
        uptimeMs:
          minimum: 0
          type: integer
      required:
      - status
      - uptimeMs
      type: object
    HealthRespBody:
      $ref: '#/components/schemas/HealthResp'
    IngestRespBody:
      $ref: '#/components/schemas/IngestResult'
    IngestResult:
      properties:
        bytes:
          minimum: 0
          type: integer
      required:
      - bytes
      type: object
    Snapshot:
      properties:
        generatedAt:
          type: string
        items:
          items:
            type: string
          type: array
        region:
          type: string
      required:
      - region
      - generatedAt
      - items
      type: object
    SnapshotReq:
      type: object
    SnapshotRespBody:
      $ref: '#/components/schemas/Snapshot'
info:
  title: Raw Control
  version: 1.0.0
openapi: 3.1.0
paths:
  /stream/download/{file}:
    get:
      description: 'Stream a file download: the request is bound and validated, logic
        serves the body with http.ServeContent.'
      operationId: Download
      parameters:
      - in: path
        name: file
        required: true
        schema:
          maxLength: 64
          pattern: ^[a-z0-9._-]+$
          type: string
      responses:
        "200":
          content:
            '*/*': {}
          description: OK
      tags:
      - stream
  /stream/events:
    get:
      description: 'Server-Sent Events firehose. The response block is a docs-only
        contract: every `data:` frame carries one Event.'
      operationId: Events
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventsRespBody'
          description: OK
      tags:
      - stream
  /stream/health:
    get:
      description: Plain JSON health check - contrasts with the raw endpoints above.
      operationId: Health
      parameters:
      - in: query
        name: verbose
        required: true
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthRespBody'
          description: OK
      tags:
      - stream
  /stream/ingest:
    post:
      description: Accept raw bytes (any content-type); the framework encodes the
        typed result.
      operationId: Ingest
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IngestRespBody'
          description: Created
      tags:
      - stream
  /stream/logs/{service}:
    get:
      description: Tail logs for a named service.
      operationId: TailLogs
      parameters:
      - in: path
        name: service
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*': {}
          description: OK
      tags:
      - stream
  /stream/metrics:
    get:
      description: Prometheus scrape endpoint (text/plain).
      operationId: Metrics
      responses:
        "200":
          content:
            '*/*': {}
          description: OK
      tags:
      - stream
  /stream/snapshot:
    get:
      description: Serve a snapshot that is cached already gzip-compressed, negotiating
        Content-Encoding per request.
      operationId: Snapshot
      parameters:
      - in: query
        name: region
        schema:
          maxLength: 8
          minLength: 2
          type:
          - string
          - "null"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SnapshotRespBody'
          description: OK
      tags:
      - stream
servers:
- url: /
