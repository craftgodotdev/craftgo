# Generated by craftgo. DO NOT EDIT.
components:
  schemas:
    Amount:
      format: int64
      minimum: 0
      type: integer
    OrderID:
      maxLength: 64
      minLength: 1
      pattern: ^[A-Za-z0-9-]+$
      type: string
    OrderPlaced:
      properties:
        orderId:
          $ref: '#/components/schemas/OrderID'
        total:
          $ref: '#/components/schemas/Amount'
      required:
      - orderId
      - total
      type: object
    OrderShipped:
      properties:
        carrier:
          minLength: 1
          type: string
        orderId:
          $ref: '#/components/schemas/OrderID'
      required:
      - orderId
      - carrier
      type: object
    Settlement:
      properties:
        amount:
          $ref: '#/components/schemas/Amount'
        orderId:
          $ref: '#/components/schemas/OrderID'
      required:
      - orderId
      - amount
      type: object
info:
  title: Brokers
  version: 1.0.0
openapi: 3.1.0
paths: {}
