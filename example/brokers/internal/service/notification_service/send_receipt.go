// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package notifications

import (
	"context"
	"fmt"

	orders "github.com/craftgodotdev/craftgo/example/brokers/internal/types/orders"

	"github.com/craftgodotdev/craftgo/example/brokers/svccontext"
	"github.com/craftgodotdev/craftgo/pkg/log"
)

// SendReceiptConsumer carries the per-message state for the SendReceipt
// consumer of NotificationService. The embedded log.Logger is bound to the
// delivery context; whether that context carries a trace depends on the
// transport - the in-process one starts a fresh context per message.
type SendReceiptConsumer struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewSendReceiptConsumer constructs a fresh consumer instance bound to ctx.
func NewSendReceiptConsumer(ctx context.Context, svcCtx *svccontext.ServiceContext) *SendReceiptConsumer {
	return &SendReceiptConsumer{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// SendReceipt handles orders.Placed. The payload arrives decoded and
// validated; returning an error tells the transport the message was not
// processed.
func (l *SendReceiptConsumer) SendReceipt(payload *orders.OrderPlaced) error {
	// The bus decoded the payload and ran its Validate() before this
	// was called, whichever broker delivered it.
	fmt.Printf("  [notifications/SendReceipt]   order=%s total=%d\n", payload.OrderID, payload.Total)
	return nil
}
