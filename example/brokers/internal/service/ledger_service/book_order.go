// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package ledger

import (
	"context"
	"fmt"

	orders "github.com/craftgodotdev/craftgo/example/brokers/internal/types/orders"

	"github.com/craftgodotdev/craftgo/example/brokers/svccontext"
	"github.com/craftgodotdev/craftgo/pkg/log"
)

// BookOrderConsumer carries the per-message state for the BookOrder
// consumer of LedgerService. The embedded log.Logger is bound to the
// delivery context; whether that context carries a trace depends on the
// transport - the in-process one starts a fresh context per message.
type BookOrderConsumer struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewBookOrderConsumer constructs a fresh consumer instance bound to ctx.
func NewBookOrderConsumer(ctx context.Context, svcCtx *svccontext.ServiceContext) *BookOrderConsumer {
	return &BookOrderConsumer{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// BookOrder handles orders.Placed. The payload arrives decoded and
// validated; returning an error tells the transport the message was not
// processed.
func (l *BookOrderConsumer) BookOrder(payload *orders.OrderPlaced) error {
	fmt.Printf("  [ledger/BookOrder]            order=%s total=%d\n", payload.OrderID, payload.Total)
	return nil
}
