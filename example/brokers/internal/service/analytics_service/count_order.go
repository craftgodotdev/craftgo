// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package analytics

import (
	"context"
	"fmt"

	orders "github.com/craftgodotdev/craftgo/example/brokers/internal/types/orders"

	"github.com/craftgodotdev/craftgo/example/brokers/svccontext"
	"github.com/craftgodotdev/craftgo/pkg/log"
)

// CountOrderConsumer carries the per-message state for the CountOrder
// consumer of AnalyticsService. The embedded log.Logger is bound to the
// delivery context; whether that context carries a trace depends on the
// transport - the in-process one starts a fresh context per message.
type CountOrderConsumer struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewCountOrderConsumer constructs a fresh consumer instance bound to ctx.
func NewCountOrderConsumer(ctx context.Context, svcCtx *svccontext.ServiceContext) *CountOrderConsumer {
	return &CountOrderConsumer{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// CountOrder handles orders.Placed. The payload arrives decoded and
// validated; returning an error tells the transport the message was not
// processed.
func (l *CountOrderConsumer) CountOrder(payload *orders.OrderPlaced) error {
	fmt.Printf("  [analytics/CountOrder]        order=%s total=%d\n", payload.OrderID, payload.Total)
	return nil
}
