// Code generated by craftgo. DO NOT EDIT.

package users

// ErrCodeAccountActionDenied is the canonical machine-readable code for AccountActionDeniedErr.
const ErrCodeAccountActionDenied = "ACCOUNT_ACTION_DENIED"

// AccountActionDeniedBody is the wire-shape payload declared at design time for AccountActionDeniedErr.
// User code instantiates this struct and hands it to NewAccountActionDeniedErr; the
// framework wraps it with the type-bound code / message metadata.
type AccountActionDeniedBody struct {
	Code   *string                 `json:"code"`
	Reason AccountSuspensionReason `json:"reason"`
}

// AccountActionDeniedErr is the typed Forbidden error generated for `AccountActionDenied`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type AccountActionDeniedErr struct {
	code    string
	message string
	AccountActionDeniedBody
}

// NewAccountActionDeniedErr constructs AccountActionDeniedErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewAccountActionDeniedErr(body AccountActionDeniedBody) *AccountActionDeniedErr {
	return &AccountActionDeniedErr{
		code:                    ErrCodeAccountActionDenied,
		message:                 "Forbidden",
		AccountActionDeniedBody: body,
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *AccountActionDeniedErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *AccountActionDeniedErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the Forbidden category.
func (e *AccountActionDeniedErr) HTTPStatus() int { return 403 }

// ErrCodeAccountSuspended is the canonical machine-readable code for AccountSuspendedErr.
const ErrCodeAccountSuspended = "ACCOUNT_SUSPENDED"

// AccountSuspendedBody is the wire-shape payload declared at design time for AccountSuspendedErr.
// User code instantiates this struct and hands it to NewAccountSuspendedErr; the
// framework wraps it with the type-bound code / message metadata.
type AccountSuspendedBody struct {
	Reason *string `json:"reason"`
	Until  *string `json:"until"`
}

// AccountSuspendedErr is the typed Forbidden error generated for `AccountSuspended`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type AccountSuspendedErr struct {
	code    string
	message string
	AccountSuspendedBody
}

// NewAccountSuspendedErr constructs AccountSuspendedErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewAccountSuspendedErr(body AccountSuspendedBody) *AccountSuspendedErr {
	return &AccountSuspendedErr{
		code:                 ErrCodeAccountSuspended,
		message:              "Forbidden",
		AccountSuspendedBody: body,
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *AccountSuspendedErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *AccountSuspendedErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the Forbidden category.
func (e *AccountSuspendedErr) HTTPStatus() int { return 403 }

// ErrCodeDependencyUnavailable is the canonical machine-readable code for DependencyUnavailableErr.
const ErrCodeDependencyUnavailable = "DEPENDENCY_UNAVAILABLE"

// DependencyUnavailableBody is the wire-shape payload declared at design time for DependencyUnavailableErr.
// User code instantiates this struct and hands it to NewDependencyUnavailableErr; the
// framework wraps it with the type-bound code / message metadata.
type DependencyUnavailableBody struct {
	Upstream string `json:"upstream"`
	Eta      *int   `json:"eta"`
}

// DependencyUnavailableErr is the typed ServiceUnavailable error generated for `DependencyUnavailable`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type DependencyUnavailableErr struct {
	code    string
	message string
	DependencyUnavailableBody
}

// NewDependencyUnavailableErr constructs DependencyUnavailableErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewDependencyUnavailableErr(body DependencyUnavailableBody) *DependencyUnavailableErr {
	return &DependencyUnavailableErr{
		code:                      ErrCodeDependencyUnavailable,
		message:                   "Service unavailable",
		DependencyUnavailableBody: body,
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *DependencyUnavailableErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *DependencyUnavailableErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the ServiceUnavailable category.
func (e *DependencyUnavailableErr) HTTPStatus() int { return 503 }

// ErrCodeEmailTaken is the canonical machine-readable code for EmailTakenErr.
const ErrCodeEmailTaken = "EMAIL_TAKEN"

// EmailTakenBody is the wire-shape payload declared at design time for EmailTakenErr.
// User code instantiates this struct and hands it to NewEmailTakenErr; the
// framework wraps it with the type-bound code / message metadata.
type EmailTakenBody struct {
	Email      string  `json:"email"`
	ExistingID *string `json:"existingId"`
}

// EmailTakenErr is the typed Conflict error generated for `EmailTaken`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type EmailTakenErr struct {
	code    string
	message string
	EmailTakenBody
}

// NewEmailTakenErr constructs EmailTakenErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewEmailTakenErr(body EmailTakenBody) *EmailTakenErr {
	return &EmailTakenErr{
		code:           ErrCodeEmailTaken,
		message:        "Conflict",
		EmailTakenBody: body,
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *EmailTakenErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *EmailTakenErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the Conflict category.
func (e *EmailTakenErr) HTTPStatus() int { return 409 }

// ErrCodeInsufficientScope is the canonical machine-readable code for InsufficientScopeErr.
const ErrCodeInsufficientScope = "INSUFFICIENT_SCOPE"

// InsufficientScopeBody is the wire-shape payload declared at design time for InsufficientScopeErr.
// User code instantiates this struct and hands it to NewInsufficientScopeErr; the
// framework wraps it with the type-bound code / message metadata.
type InsufficientScopeBody struct {
	RequiredScope string   `json:"requiredScope"`
	HoldingScopes []string `json:"holdingScopes"`
}

// InsufficientScopeErr is the typed Forbidden error generated for `InsufficientScope`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type InsufficientScopeErr struct {
	code    string
	message string
	InsufficientScopeBody
}

// NewInsufficientScopeErr constructs InsufficientScopeErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewInsufficientScopeErr(body InsufficientScopeBody) *InsufficientScopeErr {
	return &InsufficientScopeErr{
		code:                  ErrCodeInsufficientScope,
		message:               "Forbidden",
		InsufficientScopeBody: body,
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *InsufficientScopeErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *InsufficientScopeErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the Forbidden category.
func (e *InsufficientScopeErr) HTTPStatus() int { return 403 }

// ErrCodeInternalError is the canonical machine-readable code for InternalError.
const ErrCodeInternalError = "INTERNAL_ERROR"

// InternalError is the typed Internal error generated for `InternalError`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type InternalError struct {
	code    string
	message string
}

// NewInternalError constructs InternalError with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewInternalError() *InternalError {
	return &InternalError{
		code:    ErrCodeInternalError,
		message: "Internal server error",
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *InternalError) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *InternalError) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the Internal category.
func (e *InternalError) HTTPStatus() int { return 500 }

// ErrCodeInvalidToken is the canonical machine-readable code for InvalidTokenErr.
const ErrCodeInvalidToken = "INVALID_TOKEN"

// InvalidTokenErr is the typed Unauthorized error generated for `InvalidToken`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type InvalidTokenErr struct {
	code    string
	message string
}

// NewInvalidTokenErr constructs InvalidTokenErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewInvalidTokenErr() *InvalidTokenErr {
	return &InvalidTokenErr{
		code:    ErrCodeInvalidToken,
		message: "Unauthorized",
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *InvalidTokenErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *InvalidTokenErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the Unauthorized category.
func (e *InvalidTokenErr) HTTPStatus() int { return 401 }

// ErrCodeMalformedRequest is the canonical machine-readable code for MalformedRequestErr.
const ErrCodeMalformedRequest = "MALFORMED_REQUEST"

// MalformedRequestBody is the wire-shape payload declared at design time for MalformedRequestErr.
// User code instantiates this struct and hands it to NewMalformedRequestErr; the
// framework wraps it with the type-bound code / message metadata.
type MalformedRequestBody struct {
	Line   int     `json:"line"`
	Column int     `json:"column"`
	Detail *string `json:"detail"`
}

// MalformedRequestErr is the typed BadRequest error generated for `MalformedRequest`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type MalformedRequestErr struct {
	code    string
	message string
	MalformedRequestBody
}

// NewMalformedRequestErr constructs MalformedRequestErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewMalformedRequestErr(body MalformedRequestBody) *MalformedRequestErr {
	return &MalformedRequestErr{
		code:                 ErrCodeMalformedRequest,
		message:              "Bad request",
		MalformedRequestBody: body,
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *MalformedRequestErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *MalformedRequestErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the BadRequest category.
func (e *MalformedRequestErr) HTTPStatus() int { return 400 }

// ErrCodeOwnershipConflict is the canonical machine-readable code for OwnershipConflictErr.
const ErrCodeOwnershipConflict = "OWNERSHIP_CONFLICT"

// OwnershipConflictBody is the wire-shape payload declared at design time for OwnershipConflictErr.
// User code instantiates this struct and hands it to NewOwnershipConflictErr; the
// framework wraps it with the type-bound code / message metadata.
type OwnershipConflictBody struct {
	Code  *string `json:"code"`
	Owner UserRef `json:"owner"`
}

// OwnershipConflictErr is the typed Conflict error generated for `OwnershipConflict`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type OwnershipConflictErr struct {
	code    string
	message string
	OwnershipConflictBody
}

// NewOwnershipConflictErr constructs OwnershipConflictErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewOwnershipConflictErr(body OwnershipConflictBody) *OwnershipConflictErr {
	return &OwnershipConflictErr{
		code:                  ErrCodeOwnershipConflict,
		message:               "Conflict",
		OwnershipConflictBody: body,
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *OwnershipConflictErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *OwnershipConflictErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the Conflict category.
func (e *OwnershipConflictErr) HTTPStatus() int { return 409 }

// ErrCodeRateLimited is the canonical machine-readable code for RateLimitedErr.
const ErrCodeRateLimited = "RATE_LIMITED"

// RateLimitedBody is the wire-shape payload declared at design time for RateLimitedErr.
// User code instantiates this struct and hands it to NewRateLimitedErr; the
// framework wraps it with the type-bound code / message metadata.
type RateLimitedBody struct {
	Message    *string `json:"message"`
	RetryAfter int     `json:"retry_after"`
	Bucket     *string `json:"bucket"`
}

// RateLimitedErr is the typed TooManyRequests error generated for `RateLimited`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type RateLimitedErr struct {
	code    string
	message string
	RateLimitedBody
}

// NewRateLimitedErr constructs RateLimitedErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewRateLimitedErr(body RateLimitedBody) *RateLimitedErr {
	return &RateLimitedErr{
		code:            ErrCodeRateLimited,
		message:         "Too many requests",
		RateLimitedBody: body,
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *RateLimitedErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *RateLimitedErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the TooManyRequests category.
func (e *RateLimitedErr) HTTPStatus() int { return 429 }

// ErrCodeStaleVersion is the canonical machine-readable code for StaleVersionErr.
const ErrCodeStaleVersion = "STALE_VERSION"

// StaleVersionBody is the wire-shape payload declared at design time for StaleVersionErr.
// User code instantiates this struct and hands it to NewStaleVersionErr; the
// framework wraps it with the type-bound code / message metadata.
type StaleVersionBody struct {
	ExpectedVersion int `json:"expected_version"`
	ActualVersion   int `json:"actual_version"`
}

// StaleVersionErr is the typed PreconditionFailed error generated for `StaleVersion`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type StaleVersionErr struct {
	code    string
	message string
	StaleVersionBody
}

// NewStaleVersionErr constructs StaleVersionErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewStaleVersionErr(body StaleVersionBody) *StaleVersionErr {
	return &StaleVersionErr{
		code:             ErrCodeStaleVersion,
		message:          "Precondition failed",
		StaleVersionBody: body,
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *StaleVersionErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *StaleVersionErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the PreconditionFailed category.
func (e *StaleVersionErr) HTTPStatus() int { return 412 }

// ErrCodeUpstreamTimeout is the canonical machine-readable code for UpstreamTimeoutErr.
const ErrCodeUpstreamTimeout = "UPSTREAM_TIMEOUT"

// UpstreamTimeoutBody is the wire-shape payload declared at design time for UpstreamTimeoutErr.
// User code instantiates this struct and hands it to NewUpstreamTimeoutErr; the
// framework wraps it with the type-bound code / message metadata.
type UpstreamTimeoutBody struct {
	Upstream  string `json:"upstream"`
	ElapsedMs int    `json:"elapsedMs"`
}

// UpstreamTimeoutErr is the typed GatewayTimeout error generated for `UpstreamTimeout`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type UpstreamTimeoutErr struct {
	code    string
	message string
	UpstreamTimeoutBody
}

// NewUpstreamTimeoutErr constructs UpstreamTimeoutErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewUpstreamTimeoutErr(body UpstreamTimeoutBody) *UpstreamTimeoutErr {
	return &UpstreamTimeoutErr{
		code:                ErrCodeUpstreamTimeout,
		message:             "Gateway timeout",
		UpstreamTimeoutBody: body,
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *UpstreamTimeoutErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *UpstreamTimeoutErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the GatewayTimeout category.
func (e *UpstreamTimeoutErr) HTTPStatus() int { return 504 }

// ErrCodeUserGone is the canonical machine-readable code for UserGoneErr.
const ErrCodeUserGone = "USER_GONE"

// UserGoneErr is the typed Gone error generated for `UserGone`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type UserGoneErr struct {
	code    string
	message string
}

// NewUserGoneErr constructs UserGoneErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewUserGoneErr() *UserGoneErr {
	return &UserGoneErr{
		code:    ErrCodeUserGone,
		message: "Resource gone",
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *UserGoneErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *UserGoneErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the Gone category.
func (e *UserGoneErr) HTTPStatus() int { return 410 }

// ErrCodeUserNotFound is the canonical machine-readable code for UserNotFoundErr.
const ErrCodeUserNotFound = "USER_NOT_FOUND"

// UserNotFoundErr is the typed NotFound error generated for `UserNotFound`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type UserNotFoundErr struct {
	code    string
	message string
}

// NewUserNotFoundErr constructs UserNotFoundErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewUserNotFoundErr() *UserNotFoundErr {
	return &UserNotFoundErr{
		code:    ErrCodeUserNotFound,
		message: "Not found",
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *UserNotFoundErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *UserNotFoundErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the NotFound category.
func (e *UserNotFoundErr) HTTPStatus() int { return 404 }

// ErrCodeUsernameTaken is the canonical machine-readable code for UsernameTakenErr.
const ErrCodeUsernameTaken = "USERNAME_TAKEN"

// UsernameTakenBody is the wire-shape payload declared at design time for UsernameTakenErr.
// User code instantiates this struct and hands it to NewUsernameTakenErr; the
// framework wraps it with the type-bound code / message metadata.
type UsernameTakenBody struct {
	Username string `json:"username"`
}

// UsernameTakenErr is the typed Conflict error generated for `UsernameTaken`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type UsernameTakenErr struct {
	code    string
	message string
	UsernameTakenBody
}

// NewUsernameTakenErr constructs UsernameTakenErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewUsernameTakenErr(body UsernameTakenBody) *UsernameTakenErr {
	return &UsernameTakenErr{
		code:              ErrCodeUsernameTaken,
		message:           "Conflict",
		UsernameTakenBody: body,
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *UsernameTakenErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *UsernameTakenErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the Conflict category.
func (e *UsernameTakenErr) HTTPStatus() int { return 409 }

// ErrCodeValidationFailed is the canonical machine-readable code for ValidationFailedErr.
const ErrCodeValidationFailed = "VALIDATION_FAILED"

// ValidationFailedBody is the wire-shape payload declared at design time for ValidationFailedErr.
// User code instantiates this struct and hands it to NewValidationFailedErr; the
// framework wraps it with the type-bound code / message metadata.
type ValidationFailedBody struct {
	Fields []string `json:"fields"`
	Hint   *string  `json:"hint"`
}

// ValidationFailedErr is the typed UnprocessableEntity error generated for `ValidationFailed`.
// The unexported `code` and `message` fields hold the type-bound
// metadata populated by the constructor. Because they are unexported,
// json.Marshal omits them from the wire payload - clients see only
// the embedded body shape (or `{}` when no body was declared).
type ValidationFailedErr struct {
	code    string
	message string
	ValidationFailedBody
}

// NewValidationFailedErr constructs ValidationFailedErr with the framework metadata baked in.
// `code` and `message` are bound to the type and not exposed as
// constructor parameters; only the body struct varies per instance.
func NewValidationFailedErr(body ValidationFailedBody) *ValidationFailedErr {
	return &ValidationFailedErr{
		code:                 ErrCodeValidationFailed,
		message:              "Unprocessable entity",
		ValidationFailedBody: body,
	}
}

// Error implements the standard error interface and returns the
// category-default message bound to the type.
func (e *ValidationFailedErr) Error() string { return e.message }

// Code returns the machine-readable error code bound to the type.
// The transport layer reads this via an `interface{ Code() string }`
// assertion when assembling the fallback JSON envelope for errors
// whose body would otherwise marshal to `{}`.
func (e *ValidationFailedErr) Code() string { return e.code }

// HTTPStatus returns the HTTP status code associated with the UnprocessableEntity category.
func (e *ValidationFailedErr) HTTPStatus() int { return 422 }
