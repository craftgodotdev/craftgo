// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package ordersservice

import (
	"context"

	types "github.com/dropship-dev/craftgo/example/internal/types/design"

	"github.com/dropship-dev/craftgo/example/svccontext"
	"github.com/dropship-dev/craftgo/pkg/log"
)

// CancelOrderLogic carries the per-request state for the
// CancelOrder endpoint of OrdersService.
type CancelOrderLogic struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewCancelOrderLogic constructs a fresh logic instance bound to ctx.
func NewCancelOrderLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *CancelOrderLogic {
	return &CancelOrderLogic{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// CancelOrder is auth-gated. The request type combines a path id
// with a body reason — proves multi-binding in a single decl.
// CancelOrder is the business-logic entry point. Replace the
// TODO with the real implementation.
func (l *CancelOrderLogic) CancelOrder(req *types.CancelOrderReq) (*types.Order, error) {
	l.svcCtx.Lock()
	defer l.svcCtx.Unlock()
	o, ok := l.svcCtx.Orders[req.ID]
	if !ok {
		return nil, types.NewOrderNotFoundErr()
	}
	o.Status = "cancelled"
	// In a real shop the reason would land in an audit log; we just
	// reference it so the parameter isn't unused.
	_ = req.Reason
	l.svcCtx.Orders[o.ID] = o
	out := toWireOrder(o)
	return &out, nil
}
