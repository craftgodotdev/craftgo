// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package adminservice

import (
	"context"

	"github.com/dropship-dev/craftgo/example/svccontext"
	"github.com/dropship-dev/craftgo/pkg/server"
)

// StatsArrayLogic carries the per-request state for the
// StatsArray streaming endpoint of AdminService.
type StatsArrayLogic struct {
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewStatsArrayLogic constructs a fresh logic instance bound to ctx.
func NewStatsArrayLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *StatsArrayLogic {
	return &StatsArrayLogic{ctx: ctx, svcCtx: svcCtx}
}

// StatsArray streams a single JSON array, one element per tick.
// The handler picks `*server.JSONArrayStream`; logic calls
// stream.Send(...) like the other formats and stream.Close()
// at the end so the closing `]` is emitted.
// StatsArray is the streaming business-logic entry point. Replace
// the TODO with calls to stream.Send(...) — return when done.
//
// Honour l.ctx so an aborted client closes the stream cleanly:
//
//	for {
//	    select {
//	    case <-l.ctx.Done(): return l.ctx.Err()
//	    case ev := <-source:
//	        if err := stream.Send(ev); err != nil { return err }
//	    }
//	}
func (l *StatsArrayLogic) StatsArray(stream *server.JSONArrayStream) error {
	// TODO: implement
	_ = stream
	return nil
}
