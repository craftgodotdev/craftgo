// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package adminservice

import (
	"bytes"
	"context"
	"io"

	"github.com/dropship-dev/craftgo/example/svccontext"
	"github.com/dropship-dev/craftgo/pkg/log"
)

// EchoBlobLogic carries the per-request state for the
// EchoBlob raw-bytes endpoint of AdminService.
type EchoBlobLogic struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewEchoBlobLogic constructs a fresh logic instance bound to ctx.
func NewEchoBlobLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *EchoBlobLogic {
	return &EchoBlobLogic{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// EchoBlob is the raw-bytes demo. `@raw` makes the codegen skip
// JSON serde — logic receives the request body as an io.Reader
// and returns an io.Reader the handler streams back.
// EchoBlob is the raw-bytes business-logic entry point. The
// `body` parameter carries the raw request payload — read it as you
// would any io.Reader. Return an io.Reader (or io.ReadCloser, which
// the handler closes after copying) carrying the response payload.
func (l *EchoBlobLogic) EchoBlob(body io.Reader) (io.Reader, error) {
	buf, err := io.ReadAll(body)
	if err != nil {
		return nil, err
	}
	return bytes.NewReader(buf), nil
}
