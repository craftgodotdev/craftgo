// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package adminservice

import (
	"context"

	types "github.com/dropship-dev/craftgo/example/internal/types/design"

	"github.com/dropship-dev/craftgo/example/svccontext"
	"github.com/dropship-dev/craftgo/pkg/log"
)

// UploadAvatarLogic carries the per-request state for the
// UploadAvatar endpoint of AdminService.
type UploadAvatarLogic struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewUploadAvatarLogic constructs a fresh logic instance bound to ctx.
func NewUploadAvatarLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *UploadAvatarLogic {
	return &UploadAvatarLogic{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// UploadAvatar is the multipart demo. The request type carries a
// `file` field; the codegen detects it and switches to the
// multipart-form handler that calls r.ParseMultipartForm.
// UploadAvatar is the business-logic entry point. Replace the
// TODO with the real implementation.
func (l *UploadAvatarLogic) UploadAvatar(req *types.UploadAvatarReq) (*types.UploadResp, error) {
	resp := &types.UploadResp{Note: req.Note}
	if req.Avatar != nil {
		resp.Filename = req.Avatar.Filename
		resp.Size = req.Avatar.Size
	}
	return resp, nil
}
