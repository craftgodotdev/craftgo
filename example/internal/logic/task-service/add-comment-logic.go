// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package taskservice

import (
	"context"

	types "github.com/dropship-dev/craftgo/example/internal/types/tasks"

	"github.com/dropship-dev/craftgo/example/svccontext"
	"github.com/dropship-dev/craftgo/pkg/log"
)

// AddCommentLogic carries the per-request state for the
// AddComment endpoint of TaskService. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type AddCommentLogic struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewAddCommentLogic constructs a fresh logic instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewAddCommentLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *AddCommentLogic {
	return &AddCommentLogic{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// AddComment is the business-logic entry point. Replace the
// TODO with the real implementation.
func (l *AddCommentLogic) AddComment(req *types.AddCommentReq) (*types.Comment, error) {
	// TODO: implement
	return nil, nil
}
