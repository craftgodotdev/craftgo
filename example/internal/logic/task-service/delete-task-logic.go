// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package taskservice

import (
	"context"

	shared "github.com/dropship-dev/craftgo/example/internal/types/shared"
	types "github.com/dropship-dev/craftgo/example/internal/types/tasks"

	"github.com/dropship-dev/craftgo/example/svccontext"
	"github.com/dropship-dev/craftgo/pkg/log"
)

// DeleteTaskLogic carries the per-request state for the
// DeleteTask endpoint of TaskService. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type DeleteTaskLogic struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewDeleteTaskLogic constructs a fresh logic instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewDeleteTaskLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *DeleteTaskLogic {
	return &DeleteTaskLogic{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// DeleteTask is the business-logic entry point. Replace the
// TODO with the real implementation.
func (l *DeleteTaskLogic) DeleteTask(req *types.GetTaskReq) (*shared.OkResp, error) {
	// TODO: implement
	return nil, nil
}
