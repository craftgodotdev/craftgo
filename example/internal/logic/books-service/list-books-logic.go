// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package booksservice

import (
	"context"

	types "github.com/dropship-dev/craftgo/example/internal/types/design"

	"github.com/dropship-dev/craftgo/example/svccontext"
)

// ListBooksLogic carries the per-request state for the
// ListBooks endpoint of BooksService.
type ListBooksLogic struct {
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewListBooksLogic constructs a fresh logic instance bound to ctx.
func NewListBooksLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *ListBooksLogic {
	return &ListBooksLogic{ctx: ctx, svcCtx: svcCtx}
}

// ListBooks returns every book in the catalog.
// ListBooks is the business-logic entry point. Replace the
// TODO with the real implementation.
func (l *ListBooksLogic) ListBooks() (*types.BookList, error) {
	l.svcCtx.Lock()
	defer l.svcCtx.Unlock()
	items := make([]types.Book, 0, len(l.svcCtx.Books))
	for _, b := range l.svcCtx.Books {
		items = append(items, types.Book{
			ID: b.ID, Title: b.Title, Author: b.Author, Isbn: b.ISBN,
			PriceCents: b.PriceCents, Stock: b.Stock,
		})
	}
	return &types.BookList{P: types.Page[types.Book]{Items: items, Total: len(items)}}, nil
}
