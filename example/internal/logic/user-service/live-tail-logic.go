// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package userservice

import (
	"context"
	"net/http"

	"github.com/dropship-dev/craftgo/example/svccontext"
	"github.com/dropship-dev/craftgo/pkg/log"
)

// LiveTailLogic carries the per-request state for the
// LiveTail passthrough endpoint of UserService. The embedded
// log.Logger is pre-bound to the request context so logging
// surfaces trace_id / span_id / request_id.
type LiveTailLogic struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewLiveTailLogic constructs a fresh logic instance bound to ctx.
func NewLiveTailLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *LiveTailLogic {
	return &LiveTailLogic{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// LiveTail streams server logs as Server-Sent Events. The framework
// stays out of the way: logic writes `data: ...\n\n` chunks directly
// to the ResponseWriter and flushes between events.
// LiveTail is the passthrough business-logic entry point. The
// framework hands you the raw http.ResponseWriter and *http.Request
// — read path parameters via r.PathValue, write headers/body to w
// directly, and return any error to surface it through the
// framework's error writer.
func (l *LiveTailLogic) LiveTail(w http.ResponseWriter, r *http.Request) error {
	// TODO: implement
	http.Error(w, "not implemented", http.StatusNotImplemented)
	return nil
}
