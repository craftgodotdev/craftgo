// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package userservice

import (
	"context"

	shared "github.com/dropship-dev/craftgo/example/internal/types/shared"
	types "github.com/dropship-dev/craftgo/example/internal/types/users"

	"github.com/dropship-dev/craftgo/example/svccontext"
	"github.com/dropship-dev/craftgo/pkg/log"
)

// SuspendUserLogic carries the per-request state for the
// SuspendUser endpoint of UserService. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type SuspendUserLogic struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewSuspendUserLogic constructs a fresh logic instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewSuspendUserLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *SuspendUserLogic {
	return &SuspendUserLogic{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// SuspendUser is admin-only. It appends BodyLimit + Timeout to
// the primary service's middleware chain (RequestID, RateLimit,
// CORS, AuthRequired) and tightens the security scope to
// `users:admin`.
// SuspendUser is the business-logic entry point. Replace the
// TODO with the real implementation.
func (l *SuspendUserLogic) SuspendUser(req *types.GetUserReq) (*shared.OkResp, error) {
	// TODO: implement
	return nil, nil
}
