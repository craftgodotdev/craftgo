// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package userservice

import (
	"context"

	types "github.com/dropship-dev/craftgo/example/internal/types/users"

	"github.com/dropship-dev/craftgo/example/svccontext"
	"github.com/dropship-dev/craftgo/pkg/log"
)

// ListSuspendedUsersLogic carries the per-request state for the
// ListSuspendedUsers endpoint of UserService. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type ListSuspendedUsersLogic struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewListSuspendedUsersLogic constructs a fresh logic instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewListSuspendedUsersLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *ListSuspendedUsersLogic {
	return &ListSuspendedUsersLogic{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// ListSuspendedUsers reuses the canonical pagination DTO from
// shared. Keeping a dedicated `ListUsers` for the public
// surface and a separate `ListSuspendedUsers` for moderation
// lets the two endpoints diverge later (different filters,
// different rate limits) without breaking either contract.
// ListSuspendedUsers is the business-logic entry point. Replace the
// TODO with the real implementation.
func (l *ListSuspendedUsersLogic) ListSuspendedUsers(req *types.ListUsersReq) (*types.UserList, error) {
	// TODO: implement
	return nil, nil
}
