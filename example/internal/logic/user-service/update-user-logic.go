// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package userservice

import (
	"context"

	types "github.com/dropship-dev/craftgo/example/internal/types/users"

	"github.com/dropship-dev/craftgo/example/svccontext"
	"github.com/dropship-dev/craftgo/pkg/log"
)

// UpdateUserLogic carries the per-request state for the
// UpdateUser endpoint of UserService. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type UpdateUserLogic struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewUpdateUserLogic constructs a fresh logic instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewUpdateUserLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *UpdateUserLogic {
	return &UpdateUserLogic{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// UpdateUser is the business-logic entry point. Replace the
// TODO with the real implementation.
func (l *UpdateUserLogic) UpdateUser(req *types.UpdateUserReq) (*types.User, error) {
	// TODO: implement
	return nil, nil
}
