// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package projectservice

import (
	"context"

	types "github.com/dropship-dev/craftgo/example/internal/types/projects"
	shared "github.com/dropship-dev/craftgo/example/internal/types/shared"

	"github.com/dropship-dev/craftgo/example/svccontext"
	"github.com/dropship-dev/craftgo/pkg/log"
)

// AddMemberLogic carries the per-request state for the
// AddMember endpoint of ProjectService. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type AddMemberLogic struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewAddMemberLogic constructs a fresh logic instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewAddMemberLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *AddMemberLogic {
	return &AddMemberLogic{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// AddMember is the business-logic entry point. Replace the
// TODO with the real implementation.
func (l *AddMemberLogic) AddMember(req *types.AddMemberReq) (*shared.OkResp, error) {
	// TODO: implement
	return nil, nil
}
