// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package orderservice

import (
	"context"

	types "github.com/dropship-dev/craftgo/example/internal/types/orders"
	shared "github.com/dropship-dev/craftgo/example/internal/types/shared"

	"github.com/dropship-dev/craftgo/example/svccontext"
	"github.com/dropship-dev/craftgo/pkg/log"
)

// DeleteOrderLogic carries the per-request state for the
// DeleteOrder endpoint of OrderService. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type DeleteOrderLogic struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewDeleteOrderLogic constructs a fresh logic instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewDeleteOrderLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *DeleteOrderLogic {
	return &DeleteOrderLogic{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// DeleteOrder is the business-logic entry point. Replace the
// TODO with the real implementation.
func (l *DeleteOrderLogic) DeleteOrder(req *types.GetOrderReq) (*shared.OkResp, error) {
	// TODO: implement
	return nil, nil
}
