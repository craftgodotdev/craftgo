// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package orderservice

import (
	"context"

	types "github.com/dropship-dev/craftgo/example/internal/types/orders"

	"github.com/dropship-dev/craftgo/example/svccontext"
	"github.com/dropship-dev/craftgo/pkg/log"
)

// DefaultsShowcaseLogic carries the per-request state for the
// DefaultsShowcase endpoint of OrderService. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type DefaultsShowcaseLogic struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewDefaultsShowcaseLogic constructs a fresh logic instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewDefaultsShowcaseLogic(ctx context.Context, svcCtx *svccontext.ServiceContext) *DefaultsShowcaseLogic {
	return &DefaultsShowcaseLogic{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// DefaultsShowcase echoes the request back to the wire so smoke
// tests can assert which fields received their @default pre-fill
// when the client omitted them from the body.
func (l *DefaultsShowcaseLogic) DefaultsShowcase(req *types.DefaultsShowcaseReq) (*types.DefaultsShowcaseReq, error) {
	return req, nil
}
