# Generated by craftgo. DO NOT EDIT.
components:
  schemas:
    CreateGalleryReq:
      description: "CreateGalleryReq is the showcase multi-file upload: MANY files
        via\n`photos file[]`, an optional single `cover` file, and form values of
        every\nshape - required/optional text, a repeated `tags string[]` value array,
        an\nenum, a bool, and a bounded int - all in ONE multipart/form-data request,\nwith
        the album id bound from the path.\n\nOn the wire the repeated parts share
        a field name: each photo is a `photos`\npart and each tag a `tags` part, e.g.\n\n\tcurl
        -X POST http://localhost:8080/api/media/albums/sum25/gallery \\\n\t  -F \"photos=@a.jpg\"
        -F \"photos=@b.jpg\" -F \"photos=@c.jpg\" \\\n\t  -F \"cover=@hero.png\" \\\n\t
        \ -F \"title=Summer 2026\" -F \"description=Beach trip\" \\\n\t  -F \"tags=summer\"
        -F \"tags=beach\" \\\n\t  -F \"visibility=public\" -F \"featured=true\" -F
        \"priority=10\""
      properties:
        cover:
          format: binary
          type:
          - string
          - "null"
        description:
          maxLength: 2000
          type:
          - string
          - "null"
        featured:
          type: boolean
        photos:
          items:
            format: binary
            type: string
          maxItems: 20
          minItems: 1
          type: array
        priority:
          maximum: 100
          minimum: 0
          type: integer
        tags:
          items:
            type: string
          type: array
        title:
          maxLength: 200
          minLength: 1
          type: string
        visibility:
          $ref: '#/components/schemas/Visibility'
      required:
      - photos
      - title
      - tags
      - visibility
      - featured
      - priority
      type: object
    CreateGalleryReqBody:
      properties:
        cover:
          format: binary
          type:
          - string
          - "null"
        description:
          maxLength: 2000
          type:
          - string
          - "null"
        featured:
          type: boolean
        photos:
          items:
            format: binary
            type: string
          maxItems: 20
          minItems: 1
          type: array
        priority:
          maximum: 100
          minimum: 0
          type: integer
        tags:
          items:
            type: string
          type: array
        title:
          maxLength: 200
          minLength: 1
          type: string
        visibility:
          $ref: '#/components/schemas/Visibility'
      required:
      - photos
      - title
      - tags
      - visibility
      - featured
      - priority
      type: object
    CreateGalleryRespBody:
      $ref: '#/components/schemas/Gallery'
    DeleteMediaRespBody:
      $ref: '#/components/schemas/OkResp'
    Gallery:
      description: Gallery is the created gallery echoed back to the client.
      properties:
        albumId:
          maxLength: 64
          minLength: 1
          type: string
        cover:
          anyOf:
          - $ref: '#/components/schemas/GalleryPhoto'
          - type: "null"
        createdAt:
          format: date-time
          type: string
        description:
          type:
          - string
          - "null"
        featured:
          type: boolean
        id:
          maxLength: 64
          minLength: 1
          type: string
        photos:
          items:
            $ref: '#/components/schemas/GalleryPhoto'
          type: array
        priority:
          maximum: 100
          minimum: 0
          type: integer
        tags:
          items:
            type: string
          type: array
        title:
          maxLength: 200
          minLength: 1
          type: string
        visibility:
          $ref: '#/components/schemas/Visibility'
      required:
      - id
      - albumId
      - title
      - visibility
      - featured
      - priority
      - tags
      - photos
      - createdAt
      type: object
    GalleryPhoto:
      description: GalleryPhoto is one stored photo's metadata.
      properties:
        filename:
          maxLength: 256
          minLength: 1
          type: string
        id:
          maxLength: 64
          minLength: 1
          type: string
        mimeType:
          maxLength: 128
          minLength: 1
          type: string
        sha256:
          maxLength: 64
          minLength: 64
          type: string
        sizeBytes:
          minimum: 0
          type: integer
      required:
      - id
      - filename
      - sizeBytes
      - sha256
      - mimeType
      type: object
    GetMediaReq:
      type: object
    GetMediaRespBody:
      $ref: '#/components/schemas/UploadResult'
    MediaNotFoundErr:
      description: NotFound error response (HTTP 404).
      properties:
        code:
          type: string
        message:
          type: string
      required:
      - code
      - message
      type: object
    OkResp:
      properties:
        ok:
          type: boolean
      required:
      - ok
      type: object
    UploadAttachmentReq:
      properties:
        blob:
          format: binary
          type: string
      required:
      - blob
      type: object
    UploadAttachmentReqBody:
      properties:
        blob:
          format: binary
          type: string
      required:
      - blob
      type: object
    UploadAttachmentRespBody:
      $ref: '#/components/schemas/UploadResult'
    UploadAvatarReq:
      properties:
        image:
          format: binary
          type: string
      required:
      - image
      type: object
    UploadAvatarReqBody:
      properties:
        image:
          format: binary
          type: string
      required:
      - image
      type: object
    UploadAvatarRespBody:
      $ref: '#/components/schemas/UploadResult'
    UploadDocumentReq:
      properties:
        notes:
          maxLength: 2000
          type:
          - string
          - "null"
        pdf:
          format: binary
          type: string
        title:
          maxLength: 200
          minLength: 1
          type: string
      required:
      - pdf
      - title
      type: object
    UploadDocumentReqBody:
      properties:
        notes:
          maxLength: 2000
          type:
          - string
          - "null"
        pdf:
          format: binary
          type: string
        title:
          maxLength: 200
          minLength: 1
          type: string
      required:
      - pdf
      - title
      type: object
    UploadDocumentRespBody:
      $ref: '#/components/schemas/UploadResult'
    UploadResult:
      properties:
        createdAt:
          format: date-time
          type: string
        id:
          maxLength: 64
          minLength: 1
          type: string
        mimeType:
          maxLength: 128
          minLength: 1
          type: string
        sha256:
          maxLength: 64
          minLength: 64
          type: string
        sizeBytes:
          minimum: 0
          type: integer
        url:
          format: url
          maxLength: 2048
          type: string
      required:
      - id
      - url
      - sizeBytes
      - sha256
      - mimeType
      - createdAt
      type: object
    Visibility:
      description: |-
        Visibility controls who can see a gallery. A string-valued enum that rides
        the multipart body as a plain form field (wire value "private" / ...).
      enum:
      - private
      - unlisted
      - public
      type: string
info:
  title: Upload Service
  version: 1.0.0
openapi: 3.1.0
paths:
  /media/{id}:
    delete:
      description: Delete media. Idempotent.
      operationId: DeleteMedia
      parameters:
      - in: path
        name: id
        required: true
        schema:
          maxLength: 64
          minLength: 1
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteMediaRespBody'
          description: OK
      tags:
      - media
    get:
      description: Get media metadata.
      operationId: GetMedia
      parameters:
      - in: path
        name: id
        required: true
        schema:
          maxLength: 64
          minLength: 1
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetMediaRespBody'
          description: OK
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaNotFoundErr'
          description: NotFound
      tags:
      - media
  /media/albums/{albumId}/gallery:
    post:
      description: Create a gallery from a BATCH of uploaded photos. Showcases `file[]`
        (many files, 1–20) + an optional `cover` file + form values of every shape
        - text, a repeated `tags string[]`, an enum, a bool and an int - in one multipart/form-data
        request.
      operationId: CreateGallery
      parameters:
      - in: path
        name: albumId
        required: true
        schema:
          maxLength: 64
          minLength: 1
          type: string
      requestBody:
        content:
          multipart/form-data:
            encoding:
              cover:
                contentType: image/png, image/jpeg, image/webp
            schema:
              properties:
                cover:
                  format: binary
                  type: string
                description:
                  maxLength: 2000
                  type:
                  - string
                  - "null"
                featured:
                  type: boolean
                photos:
                  items:
                    format: binary
                    type: string
                  maxItems: 20
                  minItems: 1
                  type: array
                priority:
                  maximum: 100
                  minimum: 0
                  type: integer
                tags:
                  items:
                    type: string
                  type: array
                title:
                  maxLength: 200
                  minLength: 1
                  type: string
                visibility:
                  $ref: '#/components/schemas/Visibility'
              required:
              - title
              - tags
              - visibility
              - featured
              - priority
              - photos
              type: object
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateGalleryRespBody'
          description: Created
      tags:
      - media
  /media/documents:
    post:
      description: Upload a PDF document with title + optional notes.
      operationId: UploadDocument
      requestBody:
        content:
          multipart/form-data:
            encoding:
              pdf:
                contentType: application/pdf
            schema:
              properties:
                notes:
                  maxLength: 2000
                  type:
                  - string
                  - "null"
                pdf:
                  format: binary
                  type: string
                title:
                  maxLength: 200
                  minLength: 1
                  type: string
              required:
              - title
              - pdf
              type: object
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UploadDocumentRespBody'
          description: Created
      tags:
      - media
  /media/notes/{noteId}/attachments:
    post:
      description: Attach a small blob to a note (tight 500KB cap).
      operationId: UploadAttachment
      parameters:
      - in: path
        name: noteId
        required: true
        schema:
          maxLength: 64
          minLength: 1
          type: string
      requestBody:
        content:
          multipart/form-data:
            encoding:
              blob:
                contentType: image/png, text/plain, application/json
            schema:
              properties:
                blob:
                  format: binary
                  type: string
              required:
              - blob
              type: object
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UploadAttachmentRespBody'
          description: Created
      tags:
      - media
  /media/users/{userId}/avatar:
    post:
      description: Upload a user avatar (PNG/JPEG/WebP, ≤5MB).
      operationId: UploadAvatar
      parameters:
      - in: path
        name: userId
        required: true
        schema:
          maxLength: 64
          minLength: 1
          type: string
      requestBody:
        content:
          multipart/form-data:
            encoding:
              image:
                contentType: image/png, image/jpeg, image/webp
            schema:
              properties:
                image:
                  format: binary
                  type: string
              required:
              - image
              type: object
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UploadAvatarRespBody'
          description: Created
      tags:
      - media
servers:
- url: /api
