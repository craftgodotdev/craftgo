# Generated by craftgo. DO NOT EDIT.
components:
  schemas:
    MakeReqBody:
      properties:
        pri:
          $ref: '#/components/schemas/Priority'
        tir:
          $ref: '#/components/schemas/Tier'
      required:
      - pri
      - tir
      type: object
    Priority:
      enum:
      - Low
      - Normal
      - High
      type: string
    Req:
      properties:
        pri:
          $ref: '#/components/schemas/Priority'
        tir:
          $ref: '#/components/schemas/Tier'
      required:
      - pri
      - tir
      type: object
    Tier:
      enum:
      - 1
      - 2
      type: integer
info:
  title: design
  version: 0.1.0
openapi: 3.1.0
paths:
  /m:
    post:
      operationId: Make
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MakeReqBody'
        required: true
      responses:
        "204":
          description: No Content
      tags:
      - S
servers:
- url: /v1
