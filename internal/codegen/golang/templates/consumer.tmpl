// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package {{.Package}}

import (
	"context"
{{range .Imports}}
	{{.Alias}} "{{.Path}}"
{{- end}}

	"github.com/craftgodotdev/craftgo/pkg/log"
	"{{.SvccontextImport}}"
)

// {{.ConsumerType}} carries the per-message state for the {{.Name}}
// consumer of {{.Service}}. The embedded log.Logger is bound to the
// delivery context; whether that context carries a trace depends on the
// transport - the in-process one starts a fresh context per message.
type {{.ConsumerType}} struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// New{{.ConsumerType}} constructs a fresh consumer instance bound to ctx.
func New{{.ConsumerType}}(ctx context.Context, svcCtx *svccontext.ServiceContext) *{{.ConsumerType}} {
	return &{{.ConsumerType}}{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

{{- range .Doc}}
// {{.}}
{{- end}}
// {{.Name}} handles {{.Contract}}. The payload arrives decoded and
// validated; returning an error tells the transport the message was not
// processed.
func (l *{{.ConsumerType}}) {{.Name}}(payload *{{.PayloadType}}) error {
	// TODO: implement
	return nil
}
