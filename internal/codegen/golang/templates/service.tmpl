
package {{.Package}}

{{.ImportDecl}}
// {{.ServiceName}} runs {{.Service}}.{{.Method}} for one request.
type {{.ServiceName}} struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// New{{.ServiceName}} binds {{.ServiceName}} to ctx; its Logger carries ctx's trace ids.
func New{{.ServiceName}}(ctx context.Context, svcCtx *svccontext.ServiceContext) *{{.ServiceName}} {
	return &{{.ServiceName}}{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

{{range .Doc}}// {{.}}
{{end}}{{range .Entry}}// {{.}}
{{end}}func (l *{{.ServiceName}}) {{.Method}}({{.Sig.Params}}) {{.Sig.Results}} {
	// TODO: implement
{{- if .RawResponse}}
	http.Error(w, "not implemented", http.StatusNotImplemented)
	return nil
{{- else if .Sig.HasResult}}
	return nil, nil
{{- else}}
	return nil
{{- end}}
}
