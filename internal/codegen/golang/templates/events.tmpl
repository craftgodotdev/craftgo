package {{.Package}}

{{.ImportDecl}}
{{- range .Events}}
// {{.ConstName}} is the wire identity of {{.Name}}.
const {{.ConstName}} = "{{.Contract}}"

{{range .Doc}}// {{.}}
{{end}}// {{.Name}} is the {{.Contract}} event contract.
var {{.Name}} = craftevents.NewEvent[{{.PayloadType}}]({{.ConstName}}, {{.Validate}})
{{if .ValidateElems}}
// {{.Validate}} validates each element of a {{.Name}} payload.
func {{.Validate}}(items *{{.PayloadType}}) error {
	for i := range *items {
		if err := (*items)[i].Validate(); err != nil {
			return fmt.Errorf("item %d: %w", i, err)
		}
	}
	return nil
}
{{end}}{{end}}
