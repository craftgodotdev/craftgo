// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package {{.Package}}

import (
	"context"
{{- if .NeedsTypes}}

	types "{{.TypesImport}}"
{{- end}}
{{- range .ExtraTypesImports}}
	{{.Alias}} "{{.Path}}"
{{- end}}

	"github.com/dropship-dev/craftgo/pkg/log"
	"{{.SvccontextImport}}"
)

// {{.LogicName}} carries the per-request state for the
// {{.Method}} endpoint of {{.Service}}. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type {{.LogicName}} struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// New{{.LogicName}} constructs a fresh logic instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func New{{.LogicName}}(ctx context.Context, svcCtx *svccontext.ServiceContext) *{{.LogicName}} {
	return &{{.LogicName}}{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

{{- range .Doc}}
// {{.}}
{{- end}}
// {{.Method}} is the business-logic entry point. Replace the
// TODO with the real implementation.
func (l *{{.LogicName}}) {{.Method}}({{if .HasRequest}}req *{{.RequestPkgAlias}}.{{.RequestType}}{{end}}) ({{if .HasResponse}}*{{.ResponsePkgAlias}}.{{.ResponseType}}, {{end}}error) {
	// TODO: implement
{{- if .HasResponse}}
	return nil, nil
{{- else}}
	return nil
{{- end}}
}
