// Resolves the published nested modules, which carry no `replace` lines, to this tree.
// `go mod tidy` ignores workspaces, so the modules that are never published keep their `replace` lines.
go 1.26.6

use (
	.
	./example/brokers
	./example/ecommerce
	./example/grpc
	./example/raw
	./example/taskflow
	./example/todo
	./example/upload
	./pkg/events
	./pkg/events/kafka
	./pkg/events/nats
	./pkg/events/nats/internal/integration
	./pkg/wire
	./tests/e2e/matrix
)
