// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package accountservice

import (
	"context"
	shared "github.com/craftgodotdev/craftgo/tests/e2e/cornercase/internal/types/shared"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/cornercase/svccontext"
)

// LogoutService carries the per-request state for the
// Logout endpoint of AccountService. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type LogoutService struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewLogoutService constructs a fresh service instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewLogoutService(ctx context.Context, svcCtx *svccontext.ServiceContext) *LogoutService {
	return &LogoutService{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// Logout is the service entry point. Replace the
// TODO with the real implementation.
func (l *LogoutService) Logout() (*shared.ID, error) {
	// TODO: implement
	return nil, nil
}
