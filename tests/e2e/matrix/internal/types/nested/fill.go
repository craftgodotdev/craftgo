// Code generated by craftgo. DO NOT EDIT.

package nested

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *Catalog) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	emptyMap(&v.Users)
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *Cube) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	emptySlice(&v.Space)
	for i0 := range v.Space {
		emptySlice(&v.Space[i0])
		for i1 := range v.Space[i0] {
			emptySlice(&v.Space[i0][i1])
			for i2 := range v.Space[i0][i1] {
				v.Space[i0][i1][i2].FillEmpty(depth + 1)
			}
		}
	}
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *Home) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	emptySlice(&v.Rooms)
	for i0 := range v.Rooms {
		v.Rooms[i0].FillEmpty(depth + 1)
	}
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *ListPage) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	emptySlice(&v.Items)
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *Matrix) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	emptySlice(&v.Grid)
	for i0 := range v.Grid {
		emptySlice(&v.Grid[i0])
		for i1 := range v.Grid[i0] {
			v.Grid[i0][i1].FillEmpty(depth + 1)
		}
	}
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *Node) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	emptySlice(&v.Children)
	for i0 := range v.Children {
		v.Children[i0].FillEmpty(depth + 1)
	}
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *Org) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	for i0 := range v.Teams {
		v.Teams[i0].FillEmpty(depth + 1)
	}
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *Person) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	v.Home.FillEmpty(depth + 1)
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *Room) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	emptySlice(&v.Furniture)
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *Team) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	emptySlice(&v.Members)
}

// fillDepth is how deep FillEmpty enters nested values: a value cycle has no end.
const fillDepth = 1000

// emptySlice sets *s to an empty slice when it is nil.
func emptySlice[S ~[]E, E any](s *S) {
	if *s == nil {
		*s = S{}
	}
}

// emptyMap sets *m to an empty map when it is nil.
func emptyMap[M ~map[K]V, K comparable, V any](m *M) {
	if *m == nil {
		*m = M{}
	}
}
