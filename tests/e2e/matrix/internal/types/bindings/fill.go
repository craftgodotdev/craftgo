// Code generated by craftgo. DO NOT EDIT.

package bindings

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *AttachReq) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	v.FilePart.FillEmpty(depth + 1)
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *BatchUploadReq) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	emptySlice(&v.Files)
	emptySlice(&v.Tags)
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *CountsPayload) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	emptyMap(&v.Counts)
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *FilePart[T]) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	if f, ok := any(&v.Doc).(interface{ FillEmpty(int) }); ok {
		f.FillEmpty(depth + 1)
	}
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *ItemList) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	emptySlice(&v.Items)
}

// FillEmpty sets each required list, map or bytes value below v that is nil to an empty one,
// so v encodes as [] or {} there; depth counts the values entered to reach v.
func (v *InvalidInputBody) FillEmpty(depth int) {
	if depth > fillDepth {
		return
	}
	emptySlice(&v.Errors)
}

// fillDepth is how deep FillEmpty enters nested values: a value cycle has no end.
const fillDepth = 1000

// emptySlice sets *s to an empty slice when it is nil.
func emptySlice[S ~[]E, E any](s *S) {
	if *s == nil {
		*s = S{}
	}
}

// emptyMap sets *m to an empty map when it is nil.
func emptyMap[M ~map[K]V, K comparable, V any](m *M) {
	if *m == nil {
		*m = M{}
	}
}
