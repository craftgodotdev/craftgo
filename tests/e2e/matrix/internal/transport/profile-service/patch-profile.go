// Code generated by craftgo. DO NOT EDIT.

package profileservice

import (
	"net/http"

	"github.com/craftgodotdev/craftgo/pkg/server"

	service "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/service/profile-service"
	types "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/design"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// PatchProfile exercises every binding kind end-to-end. It echoes
// path id, query dryRun, header X-Idempotency-Key, cookie session,
// and a body displayName so each value's transport binding is
// verifiable.
// PatchProfile returns the http.HandlerFunc for the
// PATCH PatchProfile endpoint.
func PatchProfile(svcCtx *svccontext.ServiceContext) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		var req types.PatchProfileReq
		if err := server.JSON().Decode(r.Body, &req); err != nil {
			server.WriteValidationError(w, r, err)
			return
		}
		req.ID = r.PathValue("id")
		_q := r.URL.Query()
		if !server.RequirePresent(w, r, _q.Has("dryRun"), "dryRun", "query") {
			return
		}
		req.DryRun = _q.Get("dryRun")
		if !server.RequirePresent(w, r, len(r.Header.Values("idempotencyKey")) > 0, "idempotencyKey", "header") {
			return
		}
		req.IdempotencyKey = r.Header.Get("idempotencyKey")
		if !server.RequirePresent(w, r, server.CookiePresent(r, "sessionToken"), "sessionToken", "cookie") {
			return
		}
		if c, err := r.Cookie("sessionToken"); err == nil {
			req.SessionToken = c.Value
		}
		if err := req.Validate(); err != nil {
			server.WriteValidationError(w, r, err)
			return
		}
		l := service.NewPatchProfileService(r.Context(), svcCtx)
		resp, err := l.PatchProfile(&req)
		if err != nil {
			server.WriteError(w, r, err)
			return
		}
		w.Header().Set("Content-Type", "application/json; charset=utf-8")
		_ = server.JSON().Encode(w, resp)
	}
}
