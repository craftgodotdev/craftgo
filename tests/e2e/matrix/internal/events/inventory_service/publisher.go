// Code generated by craftgo. DO NOT EDIT.

package events

import (
	"context"

	craftevents "github.com/craftgodotdev/craftgo/pkg/events"

	types "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/events"
)

// Contract names for the events InventoryService publishes. Publisher and
// consumer both address a contract by this value.
const (
	// ItemStockedContract is the wire identity of InventoryService.ItemStocked.
	ItemStockedContract = "events.ItemStocked"
	// ShipmentDispatchedContract is the wire identity of InventoryService.ShipmentDispatched.
	ShipmentDispatchedContract = "events.ShipmentDispatched"
	// WarehouseClosedContract is the wire identity of InventoryService.WarehouseClosed.
	WarehouseClosedContract = "events.WarehouseClosed"
	// StocktakeStartedContract is the wire identity of InventoryService.StocktakeStarted.
	StocktakeStartedContract = "events.StocktakeStarted"
	// TierPromotedContract is the wire identity of InventoryService.TierPromoted.
	TierPromotedContract = "events.TierPromoted"
	// ReconciledContract is the wire identity of InventoryService.Reconciled.
	ReconciledContract = "legacy.inventory.reconciled.v2"
	// EnvelopedContract is the wire identity of InventoryService.Enveloped.
	EnvelopedContract = "events.Enveloped"
)

// Publisher publishes the events InventoryService declares.
type Publisher struct {
	bus  *craftevents.Bus
	opts []craftevents.PublishOption
}

// NewPublisher binds a Publisher to bus. opts are this publisher's
// defaults - a tenant header, a key derived the same way every time -
// applied to every message before the options of the call itself, so a
// per-call option of the same kind wins.
func NewPublisher(bus *craftevents.Bus, opts ...craftevents.PublishOption) *Publisher {
	return &Publisher{bus: bus, opts: opts}
}

// PublishItemStocked publishes InventoryService.ItemStocked as events.ItemStocked.
// Pass craftevents.WithKey(...) to order this message against others
// about the same entity; see the craftevents.PublishOption docs for the
// rest.
func (p *Publisher) PublishItemStocked(ctx context.Context, payload *types.ItemStocked, opts ...craftevents.PublishOption) error {
	return p.bus.Publish(ctx, ItemStockedContract, payload, craftevents.JoinOptions(p.opts, opts)...)
}

// PublishShipmentDispatched publishes InventoryService.ShipmentDispatched as events.ShipmentDispatched.
// Pass craftevents.WithKey(...) to order this message against others
// about the same entity; see the craftevents.PublishOption docs for the
// rest.
func (p *Publisher) PublishShipmentDispatched(ctx context.Context, payload *types.ShipmentDispatched, opts ...craftevents.PublishOption) error {
	return p.bus.Publish(ctx, ShipmentDispatchedContract, payload, craftevents.JoinOptions(p.opts, opts)...)
}

// PublishWarehouseClosed publishes InventoryService.WarehouseClosed as events.WarehouseClosed.
// Pass craftevents.WithKey(...) to order this message against others
// about the same entity; see the craftevents.PublishOption docs for the
// rest.
func (p *Publisher) PublishWarehouseClosed(ctx context.Context, payload *types.WarehouseClosed, opts ...craftevents.PublishOption) error {
	return p.bus.Publish(ctx, WarehouseClosedContract, payload, craftevents.JoinOptions(p.opts, opts)...)
}

// PublishStocktakeStarted publishes InventoryService.StocktakeStarted as events.StocktakeStarted.
// Pass craftevents.WithKey(...) to order this message against others
// about the same entity; see the craftevents.PublishOption docs for the
// rest.
func (p *Publisher) PublishStocktakeStarted(ctx context.Context, payload *types.StocktakeStarted, opts ...craftevents.PublishOption) error {
	return p.bus.Publish(ctx, StocktakeStartedContract, payload, craftevents.JoinOptions(p.opts, opts)...)
}

// PublishTierPromoted publishes InventoryService.TierPromoted as events.TierPromoted.
// Pass craftevents.WithKey(...) to order this message against others
// about the same entity; see the craftevents.PublishOption docs for the
// rest.
func (p *Publisher) PublishTierPromoted(ctx context.Context, payload *types.TierPromoted, opts ...craftevents.PublishOption) error {
	return p.bus.Publish(ctx, TierPromotedContract, payload, craftevents.JoinOptions(p.opts, opts)...)
}

// PublishReconciled publishes InventoryService.Reconciled as legacy.inventory.reconciled.v2.
// Pass craftevents.WithKey(...) to order this message against others
// about the same entity; see the craftevents.PublishOption docs for the
// rest.
func (p *Publisher) PublishReconciled(ctx context.Context, payload *types.ItemStocked, opts ...craftevents.PublishOption) error {
	return p.bus.Publish(ctx, ReconciledContract, payload, craftevents.JoinOptions(p.opts, opts)...)
}

// PublishEnveloped publishes InventoryService.Enveloped as events.Enveloped.
// Pass craftevents.WithKey(...) to order this message against others
// about the same entity; see the craftevents.PublishOption docs for the
// rest.
func (p *Publisher) PublishEnveloped(ctx context.Context, payload *types.Envelope[types.ItemStocked], opts ...craftevents.PublishOption) error {
	return p.bus.Publish(ctx, EnvelopedContract, payload, craftevents.JoinOptions(p.opts, opts)...)
}

// Batch appends InventoryService's events to a batch the caller publishes in
// one call. Obtain it from `svcCtx.Events.Batch()`; a batch may mix the
// contracts of several services, which is what an outbox drains.
type Batch struct {
	envs *[]craftevents.Envelope
	opts []craftevents.PublishOption
}

// ItemStocked adds InventoryService.ItemStocked to the batch.
func (b Batch) ItemStocked(payload *types.ItemStocked, opts ...craftevents.PublishOption) Batch {
	env := craftevents.Envelope{Event: ItemStockedContract, Payload: payload}
	env.Apply(craftevents.JoinOptions(b.opts, opts)...)
	*b.envs = append(*b.envs, env)
	return b
}

// ShipmentDispatched adds InventoryService.ShipmentDispatched to the batch.
func (b Batch) ShipmentDispatched(payload *types.ShipmentDispatched, opts ...craftevents.PublishOption) Batch {
	env := craftevents.Envelope{Event: ShipmentDispatchedContract, Payload: payload}
	env.Apply(craftevents.JoinOptions(b.opts, opts)...)
	*b.envs = append(*b.envs, env)
	return b
}

// WarehouseClosed adds InventoryService.WarehouseClosed to the batch.
func (b Batch) WarehouseClosed(payload *types.WarehouseClosed, opts ...craftevents.PublishOption) Batch {
	env := craftevents.Envelope{Event: WarehouseClosedContract, Payload: payload}
	env.Apply(craftevents.JoinOptions(b.opts, opts)...)
	*b.envs = append(*b.envs, env)
	return b
}

// StocktakeStarted adds InventoryService.StocktakeStarted to the batch.
func (b Batch) StocktakeStarted(payload *types.StocktakeStarted, opts ...craftevents.PublishOption) Batch {
	env := craftevents.Envelope{Event: StocktakeStartedContract, Payload: payload}
	env.Apply(craftevents.JoinOptions(b.opts, opts)...)
	*b.envs = append(*b.envs, env)
	return b
}

// TierPromoted adds InventoryService.TierPromoted to the batch.
func (b Batch) TierPromoted(payload *types.TierPromoted, opts ...craftevents.PublishOption) Batch {
	env := craftevents.Envelope{Event: TierPromotedContract, Payload: payload}
	env.Apply(craftevents.JoinOptions(b.opts, opts)...)
	*b.envs = append(*b.envs, env)
	return b
}

// Reconciled adds InventoryService.Reconciled to the batch.
func (b Batch) Reconciled(payload *types.ItemStocked, opts ...craftevents.PublishOption) Batch {
	env := craftevents.Envelope{Event: ReconciledContract, Payload: payload}
	env.Apply(craftevents.JoinOptions(b.opts, opts)...)
	*b.envs = append(*b.envs, env)
	return b
}

// Enveloped adds InventoryService.Enveloped to the batch.
func (b Batch) Enveloped(payload *types.Envelope[types.ItemStocked], opts ...craftevents.PublishOption) Batch {
	env := craftevents.Envelope{Event: EnvelopedContract, Payload: payload}
	env.Apply(craftevents.JoinOptions(b.opts, opts)...)
	*b.envs = append(*b.envs, env)
	return b
}

// NewBatch binds a Batch to the caller's envelope slice. opts are the
// defaults every entry starts from, the same ones [NewPublisher] takes.
func NewBatch(envs *[]craftevents.Envelope, opts ...craftevents.PublishOption) Batch {
	return Batch{envs: envs, opts: opts}
}
