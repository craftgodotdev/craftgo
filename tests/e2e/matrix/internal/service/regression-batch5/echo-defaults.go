// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package regressionbatch5

import (
	"context"

	types "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/regression"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// EchoDefaultsService carries the per-request state for the
// EchoDefaults endpoint of RegressionBatch5. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type EchoDefaultsService struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewEchoDefaultsService constructs a fresh service instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewEchoDefaultsService(ctx context.Context, svcCtx *svccontext.ServiceContext) *EchoDefaultsService {
	return &EchoDefaultsService{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// EchoDefaults is the service entry point. Replace the
// TODO with the real implementation.
func (l *EchoDefaultsService) EchoDefaults(req *types.Rg5Defaults) (*types.Rg5Defaults, error) {
	// TODO: implement
	return nil, nil
}
