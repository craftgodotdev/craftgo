// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package services

import (
	"context"

	types "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/services"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// EntryListService carries the per-request state for the
// EntryList endpoint of Ledger. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type EntryListService struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewEntryListService constructs a fresh service instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewEntryListService(ctx context.Context, svcCtx *svccontext.ServiceContext) *EntryListService {
	return &EntryListService{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// EntryList is the service entry point. Replace the
// TODO with the real implementation.
func (l *EntryListService) EntryList() (*types.ReportCards, error) {
	// TODO: implement
	return nil, nil
}
