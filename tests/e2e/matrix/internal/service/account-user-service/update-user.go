// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package accountuserservice

import (
	"context"

	types "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/services"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// UpdateUserService carries the per-request state for the
// UpdateUser endpoint of AccountUserService. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type UpdateUserService struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewUpdateUserService constructs a fresh service instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewUpdateUserService(ctx context.Context, svcCtx *svccontext.ServiceContext) *UpdateUserService {
	return &UpdateUserService{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// UpdateUser is the service entry point. Replace the
// TODO with the real implementation.
func (l *UpdateUserService) UpdateUser(req *types.AcctUpdateUserReq) (*types.AcctUser, error) {
	l.svcCtx.Lock()
	defer l.svcCtx.Unlock()
	l.svcCtx.Users[req.ID] = map[string]any{"name": req.Name, "age": req.Age, "tags": req.Tags, "meta": req.Meta}
	return &types.AcctUser{ID: req.ID, Name: req.Name, Age: req.Age, Tags: req.Tags, Meta: req.Meta}, nil
}
