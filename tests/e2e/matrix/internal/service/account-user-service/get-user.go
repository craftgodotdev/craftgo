// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package accountuserservice

import (
	"context"

	types "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/services"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// GetUserService carries the per-request state for the
// GetUser endpoint of AccountUserService. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type GetUserService struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewGetUserService constructs a fresh service instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewGetUserService(ctx context.Context, svcCtx *svccontext.ServiceContext) *GetUserService {
	return &GetUserService{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// GetUser returns the user identified by req.ID.
// GetUser is the service entry point. Replace the
// TODO with the real implementation.
func (l *GetUserService) GetUser(req *types.AcctGetUserReq) (*types.AcctUser, error) {
	l.svcCtx.Lock()
	defer l.svcCtx.Unlock()
	row, ok := l.svcCtx.Users[req.ID]
	if !ok {
		return nil, types.NewAcctUserNotFoundErr()
	}
	return rowToAcctUser(req.ID, row), nil
}

func rowToAcctUser(id string, row map[string]any) *types.AcctUser {
	u := &types.AcctUser{ID: id}
	if v, ok := row["name"].(string); ok {
		u.Name = v
	}
	if v, ok := row["age"].(*int); ok {
		u.Age = v
	}
	if v, ok := row["tags"].([]string); ok {
		u.Tags = v
	}
	if v, ok := row["meta"].(map[string]string); ok {
		u.Meta = v
	}
	return u
}
