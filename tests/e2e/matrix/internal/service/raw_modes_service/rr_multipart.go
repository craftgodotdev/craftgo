// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package rawmodes

import (
	"context"
	"net/http"

	"fmt"

	"github.com/craftgodotdev/craftgo/pkg/server"
	types "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/rawmodes"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// RrMultipartService carries the per-request state for the RrMultipart endpoint of
// RawModesService. Hand-implemented so the raw-modes runtime tests can
// observe which side of the transport the framework owns.
type RrMultipartService struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewRrMultipartService constructs a fresh service instance bound to ctx.
func NewRrMultipartService(ctx context.Context, svcCtx *svccontext.ServiceContext) *RrMultipartService {
	return &RrMultipartService{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// RrMultipart reports the parsed form the framework handed over.
func (l *RrMultipartService) RrMultipart(w http.ResponseWriter, r *http.Request, req *types.RrUploadReq) error {
	var size int64
	if req.Doc != nil {
		size = req.Doc.Size
	}
	return server.WriteBytes(w, http.StatusOK, "text/plain; charset=utf-8", []byte(fmt.Sprintf("note=%s size=%d", req.Note, size)))
}
