// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package rawmodes

import (
	"context"
	"net/http"

	"errors"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// RrLateService carries the per-request state for the RrLate endpoint of
// RawModesService. Hand-implemented so the raw-modes runtime tests can
// observe which side of the transport the framework owns.
type RrLateService struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewRrLateService constructs a fresh service instance bound to ctx.
func NewRrLateService(ctx context.Context, svcCtx *svccontext.ServiceContext) *RrLateService {
	return &RrLateService{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// RrLate commits a partial body and then fails.
func (l *RrLateService) RrLate(w http.ResponseWriter, r *http.Request) error {
	w.Header().Set("Content-Type", "text/plain; charset=utf-8")
	w.WriteHeader(http.StatusOK)
	_, _ = w.Write([]byte("partial-"))
	// The response is committed: the framework must log this error and
	// leave the wire alone rather than splice an envelope into the body.
	return errors.New("stream broke half-way")
}
