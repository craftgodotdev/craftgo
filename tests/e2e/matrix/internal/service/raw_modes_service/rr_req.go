// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package rawmodes

import (
	"context"
	"net/http"

	"github.com/craftgodotdev/craftgo/pkg/server"
	types "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/rawmodes"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// RrReqService carries the per-request state for the RrReq endpoint of
// RawModesService. Hand-implemented so the raw-modes runtime tests can
// observe which side of the transport the framework owns.
type RrReqService struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewRrReqService constructs a fresh service instance bound to ctx.
func NewRrReqService(ctx context.Context, svcCtx *svccontext.ServiceContext) *RrReqService {
	return &RrReqService{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// RrReq writes its own status and headers after the framework bound the request.
func (l *RrReqService) RrReq(w http.ResponseWriter, r *http.Request, req *types.RrReq) error {
	// req arrived bound + validated. The documented @status(201) and the
	// Location header are logic's to write - or not: 202 proves docs-only.
	w.Header().Set("X-Handled", req.ID+":"+req.Name)
	w.Header().Set("Location", "/api/raw/rr/items/"+req.ID)
	return server.WriteBytes(w, http.StatusAccepted, "text/plain; charset=utf-8", []byte("accepted"))
}
