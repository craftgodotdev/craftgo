// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package rawmodes

import (
	"context"
	"net/http"

	"fmt"

	"github.com/craftgodotdev/craftgo/pkg/server"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// PtTimeoutService carries the per-request state for the PtTimeout endpoint of
// RawModesService. Hand-implemented so the raw-modes runtime tests can
// observe which side of the transport the framework owns.
type PtTimeoutService struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewPtTimeoutService constructs a fresh service instance bound to ctx.
func NewPtTimeoutService(ctx context.Context, svcCtx *svccontext.ServiceContext) *PtTimeoutService {
	return &PtTimeoutService{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// PtTimeout reports whether @timeout put a deadline on the context.
func (l *PtTimeoutService) PtTimeout(w http.ResponseWriter, r *http.Request) error {
	_, has := r.Context().Deadline()
	return server.WriteBytes(w, http.StatusOK, "text/plain; charset=utf-8", []byte(fmt.Sprintf("deadline=%v", has)))
}
