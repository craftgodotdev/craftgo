// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package rawmodes

import (
	"context"
	"net/http"

	"github.com/craftgodotdev/craftgo/pkg/server"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// RrNoMwService carries the per-request state for the RrNoMw endpoint of
// RawModesService. Hand-implemented so the raw-modes runtime tests can
// observe which side of the transport the framework owns.
type RrNoMwService struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewRrNoMwService constructs a fresh service instance bound to ctx.
func NewRrNoMwService(ctx context.Context, svcCtx *svccontext.ServiceContext) *RrNoMwService {
	return &RrNoMwService{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// RrNoMw is a raw response outside the inherited middleware chain.
func (l *RrNoMwService) RrNoMw(w http.ResponseWriter, r *http.Request) error {
	return server.WriteBytes(w, http.StatusOK, "text/plain; charset=utf-8", []byte("nomw"))
}
