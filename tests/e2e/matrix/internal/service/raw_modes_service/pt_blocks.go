// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package rawmodes

import (
	"context"
	"net/http"

	"github.com/craftgodotdev/craftgo/pkg/server"
	types "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/rawmodes"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// PtBlocksService carries the per-request state for the PtBlocks endpoint of
// RawModesService. Hand-implemented so the raw-modes runtime tests can
// observe which side of the transport the framework owns.
type PtBlocksService struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewPtBlocksService constructs a fresh service instance bound to ctx.
func NewPtBlocksService(ctx context.Context, svcCtx *svccontext.ServiceContext) *PtBlocksService {
	return &PtBlocksService{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// PtBlocks is a passthrough whose blocks are a docs-only contract.
func (l *PtBlocksService) PtBlocks(w http.ResponseWriter, r *http.Request) error {
	// The request block is a docs-only contract: nothing was bound, so
	// logic decodes it - and can reuse the generated Validate().
	req := types.PtReq{ID: r.PathValue("id")}
	if err := req.Validate(); err != nil {
		server.WriteValidationError(w, r, err)
		return nil
	}
	w.Header().Set("Content-Type", "application/json; charset=utf-8")
	return server.JSON().Encode(w, types.PtResp{Echo: req.ID})
}
