// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package eventsubs

import (
	"context"

	events "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/events"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// NotifyDispatchConsumer carries the per-message state for the NotifyDispatch
// consumer of NotificationService. The embedded log.Logger is pre-bound to the
// message context (trace_id / span_id).
type NotifyDispatchConsumer struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewNotifyDispatchConsumer constructs a fresh consumer instance bound to ctx.
func NewNotifyDispatchConsumer(ctx context.Context, svcCtx *svccontext.ServiceContext) *NotifyDispatchConsumer {
	return &NotifyDispatchConsumer{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// NotifyDispatch handles events.ShipmentDispatched, recording the payload so the fixture's
// round-trip test can assert on what reached logic.
func (l *NotifyDispatchConsumer) NotifyDispatch(payload *events.ShipmentDispatched) error {
	l.svcCtx.Record("NotifyDispatch", payload)
	return nil
}
