// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package eventsubs

import (
	"context"

	events "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/events"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// SendStockAlertConsumer carries the per-message state for the SendStockAlert
// consumer of NotificationService. The embedded log.Logger is pre-bound to the
// message context (trace_id / span_id).
type SendStockAlertConsumer struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewSendStockAlertConsumer constructs a fresh consumer instance bound to ctx.
func NewSendStockAlertConsumer(ctx context.Context, svcCtx *svccontext.ServiceContext) *SendStockAlertConsumer {
	return &SendStockAlertConsumer{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// SendStockAlert handles events.ItemStocked, recording the payload so the fixture's
// round-trip test can assert on what reached logic.
func (l *SendStockAlertConsumer) SendStockAlert(payload *events.ItemStocked) error {
	l.svcCtx.Record("SendStockAlert", payload)
	return nil
}
