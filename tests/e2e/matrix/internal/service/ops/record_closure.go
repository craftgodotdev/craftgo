// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package eventsubs

import (
	"context"

	events "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/events"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// RecordClosureConsumer carries the per-message state for the RecordClosure
// consumer of OpsService. The embedded log.Logger is pre-bound to the
// message context (trace_id / span_id).
type RecordClosureConsumer struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewRecordClosureConsumer constructs a fresh consumer instance bound to ctx.
func NewRecordClosureConsumer(ctx context.Context, svcCtx *svccontext.ServiceContext) *RecordClosureConsumer {
	return &RecordClosureConsumer{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// RecordClosure handles events.WarehouseClosed, recording the payload so the fixture's
// round-trip test can assert on what reached logic.
func (l *RecordClosureConsumer) RecordClosure(payload *events.WarehouseClosed) error {
	l.svcCtx.Record("RecordClosure", payload)
	return nil
}
