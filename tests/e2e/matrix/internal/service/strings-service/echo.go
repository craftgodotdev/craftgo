// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package stringsservice

import (
	"context"

	types "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/strings"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// EchoService carries the per-request state for the
// Echo endpoint of StringsService. The embedded log.Logger is
// pre-bound to the request context (trace_id / span_id / request_id),
// so handlers can call l.Info(...) / l.Error(...) directly.
type EchoService struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewEchoService constructs a fresh service instance bound to ctx.
// The Logger is pulled from log.Default() (set by Server.SetLogger)
// and seeded with WithContext(ctx) so OTel trace IDs ride every line.
func NewEchoService(ctx context.Context, svcCtx *svccontext.ServiceContext) *EchoService {
	return &EchoService{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// Echo is the service entry point. Replace the
// TODO with the real implementation.
func (l *EchoService) Echo(req *types.Str_EchoReq) (*types.Str_EchoResp, error) {
	// TODO: implement
	return nil, nil
}
