// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package strings

import (
	"context"

	"github.com/craftgodotdev/craftgo/pkg/log"

	types "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/strings"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// EchoService runs StringsService.Echo for one request.
type EchoService struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewEchoService binds EchoService to ctx; its Logger carries ctx's trace ids.
func NewEchoService(ctx context.Context, svcCtx *svccontext.ServiceContext) *EchoService {
	return &EchoService{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// Echo a request that carries one field per interesting validator shape (length, pattern, format, optional, default). Logic just returns the payload after Validate() so smoke tests can assert the round-trip preserves wire fidelity.
//
// Echo implements StringsService.Echo.
func (l *EchoService) Echo(req *types.Str_EchoReq) (*types.Str_EchoResp, error) {
	// TODO: implement
	return nil, nil
}
