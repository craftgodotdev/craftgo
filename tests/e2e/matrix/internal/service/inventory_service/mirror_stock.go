// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package events

import (
	"context"

	types "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/events"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// MirrorStockConsumer carries the per-message state for the MirrorStock
// consumer of InventoryService. The embedded log.Logger is pre-bound to the
// message context (trace_id / span_id).
type MirrorStockConsumer struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewMirrorStockConsumer constructs a fresh consumer instance bound to ctx.
func NewMirrorStockConsumer(ctx context.Context, svcCtx *svccontext.ServiceContext) *MirrorStockConsumer {
	return &MirrorStockConsumer{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// MirrorStock handles events.ItemStocked, recording the payload so the fixture's
// round-trip test can assert on what reached logic.
func (l *MirrorStockConsumer) MirrorStock(payload *types.ItemStocked) error {
	l.svcCtx.Record("MirrorStock", payload)
	return nil
}
