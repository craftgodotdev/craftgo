// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package eventsubs

import (
	"context"

	events "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/events"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// GuardedStockConsumer carries the per-message state for the GuardedStock
// consumer of GuardedService. The embedded log.Logger is bound to the
// delivery context; whether that context carries a trace depends on the
// transport - the in-process one starts a fresh context per message.
type GuardedStockConsumer struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewGuardedStockConsumer constructs a fresh consumer instance bound to ctx.
func NewGuardedStockConsumer(ctx context.Context, svcCtx *svccontext.ServiceContext) *GuardedStockConsumer {
	return &GuardedStockConsumer{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// GuardedStock handles events.ItemStocked. The payload arrives decoded and
// validated; returning an error tells the transport the message was not
// processed.
func (l *GuardedStockConsumer) GuardedStock(payload *events.ItemStocked) error {
	// TODO: implement
	return nil
}
