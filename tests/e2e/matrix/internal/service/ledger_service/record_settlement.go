// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package upstream

import (
	"context"

	types "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/upstream"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// RecordSettlementConsumer carries the per-message state for the RecordSettlement
// consumer of LedgerService. The embedded log.Logger is bound to the
// delivery context; whether that context carries a trace depends on the
// transport - the in-process one starts a fresh context per message.
type RecordSettlementConsumer struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewRecordSettlementConsumer constructs a fresh consumer instance bound to ctx.
func NewRecordSettlementConsumer(ctx context.Context, svcCtx *svccontext.ServiceContext) *RecordSettlementConsumer {
	return &RecordSettlementConsumer{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// RecordSettlement handles payments.settled.v1. The payload arrives decoded and
// validated; returning an error tells the transport the message was not
// processed.
func (l *RecordSettlementConsumer) RecordSettlement(payload *types.PaymentSettledPayload) error {
	// TODO: implement
	return nil
}
