// Code generated by craftgo. DO NOT EDIT.

package regressionbatch5

import (
	"github.com/craftgodotdev/craftgo/pkg/server"
	transport "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/transport/regression-batch5"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// RegisterRoutes wires every RegressionBatch5 endpoint onto srv. Patterns
// use the Go 1.22 method-prefixed form so VERB and path match together;
// service prefix and OpenAPI basePath are baked in. Methods declaring
// `@middlewares(...)` are wrapped via the corresponding fields on
// ServiceContext (embedded Middlewares struct), so no runtime name
// lookup is required - the values come pre-wired.
func RegisterRoutes(srv *server.Server, svcCtx *svccontext.ServiceContext) {
	srv.Handle("POST /api/regression5/d", transport.EchoDefaults(svcCtx))
	srv.Handle("POST /api/regression5/bag", transport.EchoBag(svcCtx))
	srv.Handle("POST /api/regression5/comp", transport.EchoComposite(svcCtx))
	srv.Handle("POST /api/regression5/mx", transport.EchoMixed(svcCtx))
	srv.Handle("POST /api/regression5/nil", transport.EchoNilable(svcCtx))
	srv.Handle("GET /api/regression5/sess", transport.FindSession(svcCtx))
	srv.Handle("POST /api/regression5/errs", transport.DoErrors(svcCtx))
}
