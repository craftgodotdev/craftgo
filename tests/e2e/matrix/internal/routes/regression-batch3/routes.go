// Code generated by craftgo. DO NOT EDIT.

package regressionbatch3

import (
	"github.com/craftgodotdev/craftgo/pkg/server"
	transport "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/transport/regression-batch3"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// RegisterRoutes wires every RegressionBatch3 endpoint onto srv. Patterns
// use the Go 1.22 method-prefixed form so VERB and path match together;
// service prefix and OpenAPI basePath are baked in. Methods declaring
// `@middlewares(...)` are wrapped via the corresponding fields on
// ServiceContext (embedded Middlewares struct), so no runtime name
// lookup is required - the values come pre-wired.
func RegisterRoutes(srv *server.Server, svcCtx *svccontext.ServiceContext) {
	srv.Handle("POST /api/regression3/mo", transport.EchoMultipleOf(svcCtx))
	srv.Handle("POST /api/regression3/z", transport.EchoZero(svcCtx))
	srv.Handle("POST /api/regression3/d", transport.EchoDefaults(svcCtx))
	srv.Handle("POST /api/regression3/n", transport.EchoNested(svcCtx))
	srv.Handle("POST /api/regression3/r", transport.EchoResp(svcCtx))
	srv.Handle("POST /api/regression3/c", transport.EchoContact(svcCtx))
}
