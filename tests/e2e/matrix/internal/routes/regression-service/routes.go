// Code generated by craftgo. DO NOT EDIT.

package regressionservice

import (
	"github.com/craftgodotdev/craftgo/pkg/server"
	transport "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/transport/regression-service"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/svccontext"
)

// RegisterRoutes wires every RegressionService endpoint onto srv. Patterns
// use the Go 1.22 method-prefixed form so VERB and path match together;
// service prefix and OpenAPI basePath are baked in. Methods declaring
// `@middlewares(...)` are wrapped via the corresponding fields on
// ServiceContext (embedded Middlewares struct), so no runtime name
// lookup is required - the values come pre-wired.
func RegisterRoutes(srv *server.Server, svcCtx *svccontext.ServiceContext) {
	srv.Handle("POST /api/regression/nullable-scalars", transport.EchoNullableScalars(svcCtx))
	srv.Handle("POST /api/regression/ref-meta", transport.EchoRefMeta(svcCtx))
	srv.Handle("POST /api/regression/lengths", transport.EchoLengths(svcCtx))
	srv.Handle("POST /api/regression/enum-maps", transport.EchoEnumMaps(svcCtx))
	srv.Handle("POST /api/regression/host", transport.EchoHost(svcCtx))
	srv.Handle("POST /api/regression/contact", transport.EchoContact(svcCtx))
	srv.Handle("POST /api/regression/page", transport.EchoPage(svcCtx))
	srv.Handle("GET /api/regression/items/{id}/{uid}/{kind}", transport.GetItem(svcCtx))
	srv.Handle("GET /api/regression/search", transport.DoSearch(svcCtx))
}
