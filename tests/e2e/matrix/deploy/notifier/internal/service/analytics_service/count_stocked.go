// Scaffold generated by craftgo. Safe to edit; will not be overwritten.

package eventsubs

import (
	"context"

	events "github.com/craftgodotdev/craftgo/tests/e2e/matrix/internal/types/events"

	"github.com/craftgodotdev/craftgo/pkg/log"
	"github.com/craftgodotdev/craftgo/tests/e2e/matrix/deploy/notifier/svccontext"
)

// CountStockedConsumer carries the per-message state for the CountStocked
// consumer of AnalyticsService. The embedded log.Logger is bound to the
// delivery context; whether that context carries a trace depends on the
// transport - the in-process one starts a fresh context per message.
type CountStockedConsumer struct {
	log.Logger
	ctx    context.Context
	svcCtx *svccontext.ServiceContext
}

// NewCountStockedConsumer constructs a fresh consumer instance bound to ctx.
func NewCountStockedConsumer(ctx context.Context, svcCtx *svccontext.ServiceContext) *CountStockedConsumer {
	return &CountStockedConsumer{
		Logger: log.Default().WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// CountStocked handles events.ItemStocked. The payload arrives decoded and
// validated; returning an error tells the transport the message was not
// processed.
func (l *CountStockedConsumer) CountStocked(payload *events.ItemStocked) error {
	l.svcCtx.Record("CountStocked", payload)
	return nil
}
