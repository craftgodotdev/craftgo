# Generated by craftgo. DO NOT EDIT.
components:
  schemas:
    CreateUserReq:
      description: CreateUserReq is the body for POST /users.
      properties:
        age:
          type: integer
        meta:
          additionalProperties:
            type: string
          type: object
        name:
          type: string
        tags:
          items:
            type: string
          type: array
      required:
      - name
      - tags
      - meta
      type: object
    CreateUserReqBody:
      properties:
        age:
          type: integer
        meta:
          additionalProperties:
            type: string
          type: object
        name:
          type: string
        tags:
          items:
            type: string
          type: array
      required:
      - name
      - tags
      - meta
      type: object
    CreateUserRespBody:
      $ref: '#/components/schemas/User'
    DeleteUserRespBody:
      $ref: '#/components/schemas/Empty'
    Empty:
      description: |-
        Empty is the zero-payload response for endpoints that succeed without
        returning data.
      type: object
    GetUserReq:
      description: GetUserReq is the path-bound input for GET /users/{id}.
      properties:
        id:
          description: id locates the user.
          type: string
      required:
      - id
      type: object
    GetUserReqPath:
      properties:
        id:
          type: string
      required:
      - id
      type: object
    GetUserRespBody:
      $ref: '#/components/schemas/User'
    Priority:
      enum:
      - 1
      - 2
      - 3
      type: integer
    Role:
      enum:
      - admin
      - user
      - guest
      type: string
    UpdateUserReq:
      description: |-
        UpdateUserReq is the request for PUT /users/{id}. The path-bound `id`
        is required by the semantic phase to match the {id} segment; the body
        fields mirror CreateUserReq so a JSON body shaped for create works for
        update too.
      properties:
        age:
          type: integer
        id:
          type: string
        meta:
          additionalProperties:
            type: string
          type: object
        name:
          type: string
        tags:
          items:
            type: string
          type: array
      required:
      - id
      - name
      - tags
      - meta
      type: object
    UpdateUserReqBody:
      properties:
        age:
          type: integer
        meta:
          additionalProperties:
            type: string
          type: object
        name:
          type: string
        tags:
          items:
            type: string
          type: array
      required:
      - name
      - tags
      - meta
      type: object
    UpdateUserReqPath:
      properties:
        id:
          type: string
      required:
      - id
      type: object
    UpdateUserRespBody:
      $ref: '#/components/schemas/User'
    User:
      description: User is the wire-level shape of a stored user.
      properties:
        age:
          type: integer
        id:
          type: string
        meta:
          additionalProperties:
            type: string
          type: object
        name:
          type: string
        tags:
          items:
            type: string
          type: array
      required:
      - id
      - name
      - tags
      - meta
      type: object
    UserNotFoundErr:
      description: NotFound error response (HTTP 404).
      type: object
    ValidationFailedErr:
      description: BadRequest error response (HTTP 400).
      properties:
        fields:
          items:
            type: string
          type: array
      type: object
info:
  title: E2E API
  version: 1.0.0
openapi: 3.1.0
paths:
  /v1/ping:
    get:
      description: Ping is a liveness-style endpoint with no body.
      operationId: Ping
      responses:
        "204":
          description: No Content
        default:
          description: ""
      tags:
      - UserService
  /v1/users:
    post:
      description: CreateUser stores a new user and returns the freshly created entity.
      operationId: CreateUser
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserReqBody'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateUserRespBody'
          description: OK
        default:
          description: ""
      tags:
      - UserService
  /v1/users/{id}:
    delete:
      description: DeleteUser clears the in-memory store. Path id is currently ignored.
      operationId: DeleteUser
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteUserRespBody'
          description: OK
        default:
          description: ""
      tags:
      - UserService
    get:
      description: GetUser returns the user identified by req.ID.
      operationId: GetUser
      parameters:
      - in: path
        name: id
        required: true
        schema:
          $ref: '#/components/schemas/GetUserReqPath/properties/id'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetUserRespBody'
          description: OK
        default:
          description: ""
      tags:
      - UserService
    put:
      description: UpdateUser overwrites an existing user.
      operationId: UpdateUser
      parameters:
      - in: path
        name: id
        required: true
        schema:
          $ref: '#/components/schemas/UpdateUserReqPath/properties/id'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserReqBody'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateUserRespBody'
          description: OK
        default:
          description: ""
      tags:
      - UserService
servers:
- url: /api
