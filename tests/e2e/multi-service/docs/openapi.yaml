# Generated by craftgo. DO NOT EDIT.
components:
  schemas:
    FeaturedRespBody:
      $ref: '#/components/schemas/Item'
    Item:
      description: Item represents a catalog item in the catalog service.
      properties:
        price:
          type: integer
        sku:
          type: string
      type: object
    LatestRespBody:
      $ref: '#/components/schemas/Order'
    Order:
      description: Order represents a customer order in the orders service.
      properties:
        id:
          type: string
        total:
          type: integer
      type: object
    PingRespBody:
      $ref: '#/components/schemas/Pong'
    Pong:
      description: Pong is the simple response for the ping endpoints.
      properties:
        name:
          type: string
      type: object
info:
  title: Multi-Service E2E
  version: 1.0.0
openapi: 3.1.0
paths:
  /catalog/featured:
    get:
      description: Featured returns the catalog's hero item.
      operationId: Featured
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeaturedRespBody'
          description: OK
        default:
          description: ""
      tags:
      - CatalogService
  /catalog/ping:
    get:
      description: Ping the catalog service.
      operationId: Ping
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PingRespBody'
          description: OK
        default:
          description: ""
      tags:
      - CatalogService
  /orders/latest:
    get:
      description: Latest returns the most recently placed order.
      operationId: Latest
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LatestRespBody'
          description: OK
        default:
          description: ""
      tags:
      - OrdersService
  /orders/ping:
    get:
      description: Ping the orders service.
      operationId: Ping
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PingRespBody'
          description: OK
        default:
          description: ""
      tags:
      - OrdersService
servers:
- url: /api
