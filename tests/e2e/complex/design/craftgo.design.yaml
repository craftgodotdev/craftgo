output:
  types:      ./internal/types
  transport:  ./internal/transport
  routes:     ./internal/routes
  service:    ./internal/service
  svccontext: ./svccontext/svccontext.go
  openapi:    ./docs/openapi.yaml
  # Skip main.go scaffolding — this fixture is loaded as `package complex`
  # by e2e_test.go and a generated `package main` would collide.
  main:       "-"
openapi:
  title:    Profiles API
  version:  1.0.0
  basePath: /api
